import React from "react";

import styled from "styled-components";

const Box = styled.div`
  display: flex;
  border: solid 3px #f4b4d0;
  border-radius: 10px;
`;

const SearchResult = (props) => <Box></Box>;

export default SearchResult;
